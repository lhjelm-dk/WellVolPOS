# Floors, not pins. The parity suite in tests/ is what guards against drift; if a
# newer package ever changed a result, those tests would fail loudly. Pinning
# exact versions here would instead break installation on any Python version
# that lacks a matching prebuilt wheel.
numpy>=2.1
pandas>=2.2
scipy>=1.14
scikit-learn>=1.5
matplotlib>=3.9
plotly>=5.24
streamlit>=1.40
openpyxl>=3.1
pytest>=8.0
pypdf>=5.0

# Optional. Only the *plotly* report needs it: it renders the interactive figures to
# static images by driving a headless browser, which is a large download. The
# matplotlib report -- the default, and what every artefact carried before
# 2026-08-18 -- needs nothing beyond matplotlib. `export.kaleido_available()` checks
# for it and the app disables the option with a message rather than failing inside
# plotly's writer.
kaleido>=0.2.1
