# What the app needs to run. Nothing here is optional.
#
# **Floors, not pins.** The test suite is what guards against drift; pinning exact
# versions would instead break installation on any Python that lacks a matching
# prebuilt wheel. These floors match `pyproject.toml` exactly -- they were two lists
# that disagreed with each other until 2026-08-19, which is one list too many.
#
# **These floors imply Python 3.11 or newer.** numpy 2.1 and scipy 1.14 publish no
# wheels for 3.9, so an older interpreter tries to build them from source and the
# install fails. If you deploy to Streamlit Community Cloud, set the Python version
# under *Advanced settings* when you create the app -- it cannot be changed afterwards.
numpy>=2.1
pandas>=2.2
scipy>=1.14
scikit-learn>=1.5
matplotlib>=3.9
plotly>=5.24
streamlit>=1.40
openpyxl>=3.1
pypdf>=5.0
